A simple python redis client with basic very functionality (get, set, delete, exists).

Install all deps with poetry:
```sh
poetry install
```

Run tests:
```sh
poetry run pytest
```

Run example:
```sh
poetry run python example.py
```

Tags: python, redis protocol, sockets, pytest, poetry
